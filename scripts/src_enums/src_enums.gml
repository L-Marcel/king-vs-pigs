enum PIG_STATE {
	DEAD,
	IDLE,
	RUNNING,
	RUNNING_TO_BOMB,
	LIGHTING_BOMB,
	THROWING_BOMB,
	RUNNING_TO_CANNON,
	LIGHTING_CANNON,
	WILL_ATTACK,
	ATTACKING
};

enum BOMB_STATE {
	OFF,
	ON,
	BOOM
};

enum CANNON_STATE {
	OFF,
	ON
}

enum DOOR_STATE {
	OPENING,
	CLOSED,
	LOCKED,
	UNLOCKING
}

enum PLAYER_ACTION {
	IN,
	OUT,
	ATTACK,
	NONE
}

enum SWITCH_STATE {
	LEFT,
	CHANGING,
	RIGHT
};